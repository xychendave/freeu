# 🎯 FreeU 快速启动指南

## 📋 项目概述

FreeU是一个基于多AI提供商支持的本地文件整理助手，让普通用户通过自然语言指令轻松整理文件。无需编程知识，只需用日常语言描述整理需求，AI就会生成整理方案并执行。支持Claude、OpenAI、Kimi、GLM、OpenRouter等多个AI提供商。

## 🚀 快速启动

### 1. 环境要求
- Python 3.7+
- AI提供商API Key（可选，用于AI功能）
  - Claude (Anthropic API)
  - OpenAI (OpenAI API) 
  - Kimi (Moonshot API)
  - GLM (Zhipu AI API)
  - OpenRouter

### 2. 安装依赖
```bash
pip install -r requirements.txt
```

### 3. 运行演示
```bash
# 基础功能演示（无需API Key）
python3 demo_simple.py

# 完整功能演示（需要AI提供商API Key）
python3 demo.py
```

### 4. 启动应用
```bash
# 启动Web界面
python3 src/main.py

# 或使用启动脚本
./start.sh
```

访问 http://127.0.0.1:7860 使用应用

## 💡 使用示例

### 场景1：整理桌面文件
1. 选择目录：`/Users/dave/Desktop`
2. 选择AI提供商（如Claude、OpenAI、Kimi等）
3. 输入指令：`把图片放到Pictures，文档放到Documents`
4. 查看预览，确认无误后执行

### 场景2：按文件类型分类
1. 选择下载文件夹
2. 输入指令：`按文件类型分类整理`
3. AI会自动识别文件类型并分类

### 场景3：清理重复文件
1. 选择需要整理的目录
2. 输入指令：`找出重复的文件`
3. 查看AI识别的重复文件列表

## 🔧 核心功能

### ✅ 已实现功能
- **文件扫描**：递归/非递归扫描目录，支持大目录扫描（最多10000+文件）
- **文件操作**：安全的文件移动操作
- **路径安全检查**：防止操作到系统文件，可配置排除规则
- **日志系统**：详细的操作记录，实时扫描进度显示
- **错误处理**：完善的异常处理
- **预览确认**：执行前展示操作列表
- **Web界面**：用户友好的Gradio界面，支持扫描设置
- **多AI支持**：支持Claude、OpenAI、Kimi、GLM、OpenRouter
- **扫描优化**：可配置文件数量限制，支持无限制扫描模式

### 🚧 开发中功能
- **AI集成**：多AI提供商API集成（需要API Key）
- **桌面打包**：Electron应用打包
- **多语言支持**：中英文界面
- **撤销功能**：操作回滚

## 🛡️ 安全特性

- **只移动不删除**：保护用户文件安全
- **路径白名单**：只操作用户指定目录
- **系统文件保护**：自动跳过系统文件
- **隐藏文件跳过**：不处理隐藏文件
- **权限检查**：执行前验证文件权限

## 📁 项目结构

```
freeu/
├── src/                    # 源代码
│   ├── core/               # 核心功能
│   │   ├── scanner.py      # 文件扫描器
│   │   ├── ai_engine.py    # AI引擎
│   │   └── file_executor.py # 文件执行器
│   ├── ui/                 # 用户界面
│   │   └── gradio_interface.py # Gradio界面
│   ├── utils/              # 工具模块
│   │   ├── config.py       # 配置管理
│   │   └── logger.py       # 日志系统
│   └── main.py             # 应用入口
├── electron/               # Electron桌面打包
├── tests/                  # 测试文件
├── scripts/                # 构建脚本
├── requirements.txt        # Python依赖
└── README.md              # 项目说明
```

## 🔑 配置AI提供商

FreeU现在支持多个AI提供商，您可以根据自己拥有的API Key选择使用：

### 支持的AI提供商
- **Claude** (Anthropic API)
- **OpenAI** (OpenAI API) 
- **Kimi** (Moonshot API)
- **GLM** (Zhipu AI API)
- **OpenRouter** (OpenRouter API)

### 方法1：Web界面配置（推荐）
1. 启动应用后，点击"AI设置"标签页
2. 选择AI提供商
3. 输入对应的API Key
4. 点击"保存配置"

### 方法2：配置文件
```bash
mkdir -p ~/.freeu
cat > ~/.freeu/config.json << EOF
{
    "ai_provider": "claude",
    "ai_providers": {
        "claude": {
            "api_key": "your-anthropic-api-key",
            "model": "claude-3-5-sonnet-20241022",
            "enabled": true
        },
        "openai": {
            "api_key": "your-openai-api-key", 
            "model": "gpt-4-turbo-preview",
            "enabled": false
        },
        "kimi": {
            "api_key": "your-moonshot-api-key",
            "model": "moonshot-v1-8k",
            "enabled": false
        },
        "glm": {
            "api_key": "your-zhipu-api-key",
            "model": "glm-4",
            "enabled": false
        },
        "openrouter": {
            "api_key": "your-openrouter-api-key",
            "model": "anthropic/claude-3.5-sonnet",
            "enabled": false
        }
    }
}
EOF
```

### 方法3：环境变量
```bash
export ANTHROPIC_API_KEY="your-anthropic-api-key"
export OPENAI_API_KEY="your-openai-api-key"
export MOONSHOT_API_KEY="your-moonshot-api-key"
export ZHIPU_API_KEY="your-zhipu-api-key"
export OPENROUTER_API_KEY="your-openrouter-api-key"
```

### 方法4：首次启动时输入
应用首次启动时会提示配置AI提供商

## 🧪 测试

```bash
# 运行基础测试
python3 test_basic.py

# 运行完整测试
python3 tests/test_core.py
```

## 📊 开发进度

### Phase 1：基础框架 ✅
- [x] 项目结构搭建
- [x] 文件扫描模块
- [x] 配置管理系统
- [x] 日志系统

### Phase 2：核心功能 ✅
- [x] 文件操作执行
- [x] 路径安全检查
- [x] 错误处理机制
- [x] Gradio Web界面

### Phase 3：AI集成 ✅
- [x] Claude API集成
- [x] OpenAI API集成 ✅ 新增
- [x] Kimi API集成 ✅ 新增
- [x] GLM API集成 ✅ 新增
- [x] OpenRouter API集成 ✅ 新增
- [x] 多AI提供商支持 ✅ 新增
- [x] AI配置管理界面 ✅ 新增
- [x] 自然语言处理
- [x] 方案生成与验证
- [ ] 多轮对话支持

### Phase 4：桌面应用 🚧
- [x] Electron集成
- [x] 应用打包脚本
- [ ] macOS应用签名
- [ ] Windows安装包

## 🐛 常见问题

### Q1: 安装依赖失败
**解决方案**：
```bash
# 升级pip
pip install --upgrade pip

# 使用国内镜像
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### Q2: AI API调用失败
**解决方案**：
1. 检查API Key是否正确配置
2. 确认网络连接正常
3. 查看日志文件了解详细错误
4. 尝试切换其他AI提供商
5. 在AI设置页面测试连接

### Q3: 文件操作权限不足
**解决方案**：
1. 检查目标目录权限
2. 以管理员权限运行应用
3. 确保不操作系统保护目录

## 📞 支持

- 📧 邮箱支持：davechenhello@gmail.com
- 💬 社区讨论：https://github.com/FreeU-group
- 🐛 问题反馈：在GitHub提交Issue

## 📄 许可证

MIT License - 详见 [LICENSE](LICENSE) 文件

---

**🎉 享受智能文件整理的乐趣吧！**